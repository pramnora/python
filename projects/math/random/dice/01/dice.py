import random               # import random
                            # which is a 'built-in' Python library
                            # that can be used to produce random numbers

print(random.randint(1,6))  # print a single dice, 
                            # using method: 'random.randint(minNo,maxNo)'
                            # to create a dice displaying a random number: 1-6

